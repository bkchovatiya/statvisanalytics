<%= form_for(place_rating, url: path, method: :post) do |form| %>
  <% if place_rating.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(place_rating.errors.count, "error") %> prohibited this place_rating from being saved:</h2>

      <ul>
        <% place_rating.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :rating, style: "display: block" %>
    <%= form.number_field :rating %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
